#include <stdio.h>
#include <stdlib.h>
#include<string.h>

int main()
{
  char name[14] = "kabir idli";
  printf("My name is %s \n", name);
  
  char food[] = "idli";
  printf("The best food is %s \n", food);
  
  strcpy(food, "dosa");
  printf("The best food is %s \n", food);
  
  return 0;
}
